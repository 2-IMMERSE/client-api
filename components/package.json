{
  "name": "DMAppComponents",
  "version": "0.0.1",
  "dependencies": {
    "dashjs": "^2.9.0"
  },
  "scripts": {
    "postinstall": "mkdir -p deps/ && cp ./node_modules/dashjs/dist/dash.all.min.js* deps/"
  }
}
